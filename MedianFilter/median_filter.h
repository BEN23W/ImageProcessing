#include<stdint.h>
#include<cstdio>

/* Noise Removal from Image using Median Filter
**Median Filters are useful for salt and pepper noise and
**preserving edges

** Using a 3x3 sampling window */

struct Median{

    void InsertionSort(int arr, int n);
    


};

